# wuji-sdk：2026.7.21 / 8.3 起 retargeting 原生化——无需 [retarget] extra，
#   HandModel / RetargetSession 顶层导入，提供 cp312 + cp313 wheel（Python 3.12 / 3.13 均可）。
#   ≤7.15 才需 `wuji-sdk[retarget]` 且只有 cp312（Python 3.12）。仅 Linux x86_64/aarch64。
#   本仓 retarget_compat.py 已兼容新旧两种导入路径。
wuji-sdk
mujoco
imageio
imageio-ffmpeg
opencv-python
numpy
